# kBar 需求说明（更新于 2026-04-05）

## 背景

在 MacBook Air 等小屏或刘海屏设备上，右上角菜单栏图标容易拥挤，部分第三方图标不易点击。  
核心诉求是：保持原始菜单栏不变的前提下，提供一个可操作的“虚拟菜单栏”入口，方便点击这些图标。

## 产品目标

实现一个 macOS 菜单栏工具 `kBar`，点击 `kBar` 图标后，在刘海下方（或其他合适位置）弹出悬浮面板，展示右上角菜单栏中当前可见的第三方图标，以及被 macOS 因空间不足隐藏但仍可探测的第三方图标，并尽量保持原始交互体验。

## 核心需求（V2）

1. `kBar` 常驻菜单栏，作为虚拟菜单栏入口。
2. 点击 `kBar` 后以开关方式控制悬浮面板：未弹出时打开，已弹出时再次点击收起。
3. 面板内图标支持原始交互转发：
   - 左键点击
   - 右键点击
4. 扫描与展示优先聚焦 `kBar` 左侧区域，避免 `kBar` 被系统挤进刘海后难以使用。
5. 不遮挡、不隐藏、不修改原始菜单栏图标布局。
6. 图标视觉尽量与原始菜单栏图标一致，但不阻塞主流程交付。
7. 当前阶段重点要补齐“被 macOS 因空间不足而隐藏的第三方菜单栏图标”发现能力。
8. 在隐藏图标能力开发前，必须先具备可开关的本地可观测性日志能力；默认关闭，需要排查时可显式开启。
9. 当 `kBar` 本身被 macOS 挤出可见菜单栏时，应用仍需提供可用的备用入口；当前确认的方案是全局快捷键唤起虚拟菜单栏，并支持用户在设置页中自定义快捷键。
10. 当前台应用已切换、缓存已失效时，面板不能先展示上一份虚拟菜单栏结果；应直接刷新，或短暂展示加载态后再展示最新结果。

## 非目标（当前阶段不做）

1. 不再实现“遮挡原始菜单栏”能力。
2. 不承诺“视觉上只剩 kBar 图标”。
3. 不承诺 100% 兼容所有第三方菜单栏应用。

## 当前实现状态（截至 2026-04-05）

1. `kBar` 左键已支持虚拟菜单栏开关：首次点击打开，再次点击收起。
2. 面板打开链路已做性能优化，点击后会优先显示面板，避免被同步扫描阻塞而产生明显卡顿。
3. 当前真实菜单栏可见区域中的多个第三方图标，已经可以在面板中展示并触发点击。
4. 被 macOS 隐藏的第三方图标发现链路已经打通，当前已在真实样本上验证 `Bob`、`OrbStack` 可被识别并出现在面板中。
5. 隐藏图标已支持独立 logo 展示；优先使用宿主 App 图标，必要时再回退到菜单栏裁图或占位图。
6. 左键 / 右键交互转发主链路已经打通；对已验证的隐藏样本，点击后可弹出原始菜单，且不再出现“菜单短暂弹出后自动关闭”的问题。
7. 项目根目录保留可选诊断落盘能力，但默认关闭；需要排查时可通过代码常量显式开启。
8. 当前阶段的大问题已经从“能否发现隐藏图标”转为“如何扩大隐藏图标兼容性，并减少对交互回退链路的依赖”。
9. 已新增可配置的全局快捷键兜底入口，默认是 `⌃⌥⌘K`；用户可在设置页通过键盘录制修改，配置会持久化保存。
10. 已修复全局快捷键唤起时面板跟随鼠标位置漂移的问题；当前会优先锚定真实 `kBar` 图标，若 `kBar` 已被系统隐藏，则稳定回退到目标屏幕右上角下方。
11. 已补齐前台应用切换后的缓存失效处理：当前台应用变化后，面板会优先刷新最新结果；若刷新尚未完成，则短暂展示“正在刷新...”而不是先闪出旧面板。
12. 已修复“在设置页修改全局快捷键后，虚拟菜单栏被前台 App 文本菜单污染”的问题；当前当 `kBar` 自己临时成为前台应用时，会跳过这类刷新，并在切回外部 App 后强制补一次正确刷新。

## 已知限制 / 待后续研究

1. 当前版本主要基于“菜单栏可见区域截图 + AX 信息补充”识别候选图标。
2. 对隐藏图标，当前新增了宿主进程级 `AXExtrasMenuBar` / `runningAppExtras` 探测，因此虚拟菜单栏已经不再只覆盖“系统当前仍显示在真实菜单栏中的图标”。
3. 但当前机器上 `SystemUIServer` 根节点仍然不可用，隐藏图标发现主要依赖宿主进程 AX 探测路径。
4. 部分隐藏图标虽然能被发现，但直接 `AXPress` 可能返回 `cannotComplete`，此时仍需依赖回退交互链路，不保证对所有第三方菜单栏应用都稳定有效。
5. 在后续兼容性扩展过程中，仍需持续记录候选来源、过滤原因、AX 树探测结果与失败原因。
6. 公开 API 下不能可靠地把第三方 `NSStatusItem` 固定到系统菜单栏最右侧，因此不能依赖“排序控制”来保证 `kBar` 始终可见。

## 权限需求

1. 辅助功能权限：用于 AX 元素发现与交互转发。
2. 录屏权限：用于菜单栏截图扫描与图标快照。

## 诊断开关与代码常量

1. 诊断总开关位于 `kBar/Core/Utils/DiagnosticsFileLogger.swift` 的 `DiagnosticsConfiguration.isEnabled`，默认是 `false`。改为 `true` 后重新构建，项目根目录会开始写入 `kbar-events.log`、`kbar-scan-latest.log`、`kbar-interaction-latest.log`、`kbar-hidden-discovery-latest.log`。
2. 扫描图片导出开关位于同一处的 `DiagnosticsConfiguration.isScanImageExportEnabled`，默认是 `false`。只有在 `isEnabled = true` 时它才生效，用于额外导出菜单栏原始截图与候选标注图，便于排查扫描候选、过滤结果和裁图问题。
3. 扫描图片导出文件位于系统临时目录，前缀分别为 `kbar-menubar-capture-*.png`、`kbar-menubar-annotated-*.png`。
4. 用法：需要排查时，直接把这两个代码常量改成目标值并重新构建；排查结束后再恢复为默认的 `false`。

## UI 参考

1. 菜单栏参考图：`kbar开启后的预期菜单栏.png`
2. 弹窗参考图：`点击kbar预期UI效果.png`

## 验收口径（MVP）

1. 启动后不影响系统原始菜单栏可见性与行为。
2. 点击 `kBar` 后可稳定打开虚拟菜单栏，面板已显示时再次点击可稳定收起。
3. 面板内图标可触发常见第三方应用的左键 / 右键行为。
4. 权限缺失时有明确提示与重试入口。
5. 当前阶段的新增重点验收项是：隐藏图标发现链路具备可开关的可观测性日志能力；在显式开启诊断后，能够稳定输出足以支撑调试的诊断结果。
6. 当前阶段的核心功能验收项是：至少一组真实隐藏图标样本可被稳定发现、展示，并可通过面板触发原始菜单。
7. 当 `kBar` 被系统隐藏时，用户仍可通过设置页中配置的全局快捷键唤起虚拟菜单栏；默认快捷键为 `⌃⌥⌘K`。
8. 全局快捷键唤起时，面板位置稳定：`kBar` 可见则锚定到真实图标下方，`kBar` 被隐藏则稳定出现在目标屏幕右上角下方，而不是跟随鼠标漂移。
9. 当前台应用发生变化后，用户再次打开面板时，不会先看到上一前台应用的虚拟菜单栏结果；必要时仅允许短暂出现“正在刷新...”加载态。
10. 用户在设置页修改全局快捷键后，再回到外部 App 使用新快捷键唤起面板时，不会把前台 App 的文本菜单项错误带入虚拟菜单栏。

# kBar 需求说明（更新于 2026-04-02）

## 背景

在 MacBook Air 等小屏或刘海屏设备上，右上角菜单栏图标容易拥挤，部分第三方图标不易点击。  
核心诉求是：保持原始菜单栏不变的前提下，提供一个可操作的“虚拟菜单栏”入口，方便点击这些图标。

## 产品目标

实现一个 macOS 菜单栏工具 `kBar`，点击 `kBar` 图标后，在刘海下方（或其他合适位置）弹出悬浮面板，展示右上角菜单栏中当前可见的第三方图标，并尽量保持原始交互体验。

## 核心需求（V2）

1. `kBar` 常驻菜单栏，作为虚拟菜单栏入口。
2. 点击 `kBar` 后弹出悬浮面板，展示识别到的第三方菜单栏图标。
3. 面板内图标支持原始交互转发：
   - 左键点击
   - 右键点击
4. 扫描与展示优先聚焦 `kBar` 左侧区域，避免 `kBar` 被系统挤进刘海后难以使用。
5. 不遮挡、不隐藏、不修改原始菜单栏图标布局。
6. 图标视觉尽量与原始菜单栏图标一致，但不阻塞主流程交付。

## 非目标（当前阶段不做）

1. 不再实现“遮挡原始菜单栏”能力。
2. 不承诺“视觉上只剩 kBar 图标”。
3. 不承诺 100% 兼容所有第三方菜单栏应用。
4. 当前阶段不解决“被 macOS 因屏幕宽度 / 刘海空间不足而彻底隐藏的菜单栏图标”发现问题。

## 当前实现状态（截至 2026-04-02）

1. `kBar` 左键可直接弹出虚拟菜单栏。
2. 当前真实菜单栏可见区域中的多个第三方图标，已经可以在面板中展示并触发点击。
3. 左键 / 右键交互转发主链路已经打通。
4. 设置页与项目根目录都已提供诊断能力，便于定位扫描和交互问题。
5. 当前唯一明确未完成的大问题，是“系统彻底隐藏图标”的发现能力。

## 已知限制 / 待后续研究

1. 当前版本主要基于“菜单栏可见区域截图 + AX 信息补充”识别候选图标。
2. 当菜单栏图标过多、系统只展示右侧有限数量图标时，超出可见区域、被 macOS 完全隐藏的第三方图标不会出现在截图中。
3. 这意味着：当前虚拟菜单栏只能覆盖“系统当前仍显示在真实菜单栏中的图标”，无法直接拿到那些尚未补位显示的隐藏图标。
4. 该问题属于后续能力建设项，可能需要研究更深层的状态栏枚举、宿主进程级发现方案，或其他替代路线；本阶段先记录，不作为当前交付阻塞项。

## 权限需求

1. 辅助功能权限：用于 AX 元素发现与交互转发。
2. 录屏权限：用于菜单栏截图扫描与图标快照。

## UI 参考

1. 菜单栏参考图：`kbar开启后的预期菜单栏.png`
2. 弹窗参考图：`点击kbar预期UI效果.png`

## 验收口径（MVP）

1. 启动后不影响系统原始菜单栏可见性与行为。
2. 点击 `kBar` 后可稳定弹出虚拟菜单栏。
3. 面板内图标可触发常见第三方应用的左键 / 右键行为。
4. 权限缺失时有明确提示与重试入口。
5. 当前阶段允许“系统彻底隐藏图标”暂不覆盖，但需要在文档中明确记录。
